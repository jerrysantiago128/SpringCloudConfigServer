version: '3.8'

services:
  springboot-app:
    container_name: secure-test-app-ssl
    hostname: secure-test-app-ssl
    image: secure-test-app:ssl
    ports:
      - "8080:8080"
    networks:
      - cloud-config-network
    restart: always

networks:
  cloud-config-network:
    external: true
